# dot files
.DS_Store

# Node.js
node_modules
pnpm-lock.yaml
yarn.lock

# TSC
*.tsbuildinfo

# Scaffold
.env
.scaffold
build
logs

# Build artifacts (distributed via GitHub Releases, not committed)
*.xpi

# Isolated dev Zotero profile + data dir (regenerated by `npm start`/`npm test`).
# Can grow to hundreds of MB (Zotero cache + a synced copy of your library).
.dev