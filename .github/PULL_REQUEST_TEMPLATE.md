<!-- Thanks for contributing to Zotero Triage! -->

## What & why

<!-- What does this change, and what problem does it solve? Link any related issue: "Closes #123". -->

## How it was tested

<!-- The unit tests run inside Zotero (`npm test` needs a real Zotero + .env).
     At minimum, confirm `npm run build` and `npm run lint:check` pass, and
     describe any manual testing in a dev Zotero profile. -->

- [ ] `npm run build` passes (includes `tsc --noEmit`)
- [ ] `npm run lint:check` passes
- [ ] Behavior verified in a dev Zotero (`npm start`) — describe below

## Notes

<!-- Anything reviewers should know: trade-offs, follow-ups, screenshots/GIFs for UI changes. -->
